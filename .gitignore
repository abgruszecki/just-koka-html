.logs--codex
.koka
.build
tools/__pycache__
tools/.pytest_cache
## Julia
# Test manifests often capture absolute `path = ...` entries (e.g. local dev checkout),
# which breaks CI checkouts in a different directory.
test/Manifest.toml
